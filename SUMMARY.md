# 三模型光计算迁移实验 · 总结报告

> 本文档由 `EXPERIMENTS.md` 浓缩而成，面向报告/答辩使用。
> 主线：三个 EuroSAT (10 类遥感, 64×64) CNN → **Gazelle 光计算硬件 (8×2 光学矩阵乘法器)** 的量化迁移。
> 聚焦维度：**实现方式 / 参数量 / 参数精度 / 准确率 / 运行速率**。
> 数据基准：Bug #11 (test⊂train 泄漏) 修复后的**干净三分 split** (train 16200 / val 5400 / test 5400)；历史 leaky 数字一律标注作废。

---

## 1. 一句话结论

- **Model 1 (VGG)**：参数量最大 (~2.39M)、精度最高，但计算量是另两个模型的 **~150×**，真机全量不可行（~9 天）。
- **Model 2/3 (SpaceNet)**：参数量仅 ~268K，**真机 int8 全量 5400 张 ≈ 90.4% / 90.3%，单张 ~2.5s**，是可落地部署的轻量主力。
- **量化配方定型**：`stem 首层 FP32 (电计算) + 其余 Conv/Linear int8 + Gazelle 噪声训练`，与 osimulator 8a8w 推理路径天然对齐，真机 gap 仅 ~1.6 点。
- **KD 不带来真机增益**：Model 2 (无 KD) 与 Model 3 (KD) 真机精度打平 (90.43% vs 90.28%)、硬件 gap 基本相等 (~1.6 点)。

---

## 2. 硬件平台 (Gazelle，2026-07-09 逆向确认)

| 参数 | 值 | 影响 |
|---|---|---|
| 物理 tile | **8×2** (k=8, n=2) | im2col 展平长度须被 8 整除 |
| 原生精度 | **8a8w12o** (8-bit 激活 / 8-bit 权重 / 12-bit 输出) | 硬件支持 int8，不必退守 int4 |
| DAC ENOB | 7.5 bits | 训练噪声匹配项 |
| TIA 噪声 σ | ≈5.3e-4 (MSE 2.85e-7) | 训练噪声匹配项 |
| 硬件线性度 | **99.4%** (相对误差 0.6%) | 硬件近乎理想，量化精度是唯一瓶颈 |

> **关键认知**：硬件本身几乎理想（线性度 99.4%），所有精度损失来自量化与噪声建模，而非硬件缺陷。

---

## 3. 三模型架构与实现方式（横向总览）

| | Model 1 | Model 2 | Model 3 |
|---|---|---|---|
| **名称** | Baseline VGG | OpticSpaceNet V1 | OpticSpaceNet V2 |
| **设计思路** | 标准 CNN 基线 | 硬件对齐 (2×2 conv) | 硬件对齐 + **知识蒸馏 (KD)** |
| **结构** | 6× Conv2d (3×3) + 2× Linear | 4× Conv2d (1×1/2×2) + 2× Linear | 同 Model 2 |
| **参数量** | **~2.39M** | **~268K** | **~268K** |
| **训练方式** | 标准分类 | 标准分类 | KD (教师 ResNet-18, 97.83%; T=4.0, α=0.7) |
| **bias** | False (Phase4+) | False | False |
| **BN** | 保留 (FP32 运行) | 保留 | 保留 |
| **硬件对齐率** | 99.8% (首层 84.4%) | 99.6% (stem 37.5%) | 99.6% (同 M2) |
| **总 MOPs/张** | **156.63M** | **1.05M** | **1.05M** |

> **架构差异本质**：Model 1 是大而精的通用基线；Model 2/3 为硬件量身定做（2×2 conv 使展平长度完美对齐 8×2 tile），用 ~1/9 的参数换 ~1/150 的计算量。

---

## 4. 纵向发展：各模型量化精度演进

> 横轴为量化方案迭代，纵轴为精度。`val` = 干净验证集；标注「作废」者为 Bug #11 leaky 数据。

### 4.1 Model 1 (Baseline VGG，FP32 基准 97.17%)

| Phase | 实现方式 | 参数精度 | 精度 | vs FP32 | 判定 |
|---|---|---|---|---|---|
| 0 | FP32 基准 | fp32 | 97.17% | — | — |
| 1 | QAT 微调 (FP32→低比特) | int4 | 85.91% | −11.26% | ✗ |
| 2 | 从零 QAT | int4 | 91.17% | −6.00% | △ |
| 3 | LSQ+ (旧版有 bug) | int4 | 61.72% | −35.45% | ✗ |
| 4 | STE + 噪声注入 | int4 | 96.46% | −0.71% | ✓ |
| 5 | **Mixed** (Conv int4 / Linear fp32) | 混合 | **98.26%** | **+1.09%** | ★★ |
| 6 | v3 Gazelle 匹配 (变体 A, conv1_1 电计算) | int8 | **97.87%** (val) | +0.70% | ★ |
| 6 | v3 Gazelle 匹配 (变体 B, conv1_1+conv3_2 电计算) | int8 | **98.02%** (val) | +0.85% | ★ |

**Model 1 轨迹要点**：微调路线 (Phase 1) 彻底失败；从零 QAT (Phase 2) 有效但仍差 6%；STE (Phase 4) 把 int4 损失压到 0.71%；Mixed (Phase 5) 与 int8 v3 (Phase 6) **双双反超 FP32 基准**。变体 A/B 统计打平 (Δ 0.15%)，B 多回退一层到电计算、精度不降反微升、osim 提速 ~24% → **性价比更高**。

### 4.2 Model 2 (SpaceNet V1，FP32 基准 90.15%)

| Phase | 实现方式 | 参数精度 | 精度 | vs FP32 | 判定 |
|---|---|---|---|---|---|
| 0 | FP32 基准 | fp32 | 90.15% | — | — |
| 1 | QAT 微调 | int4 | 73.63% | −16.52% | ✗ |
| 2 | 从零 QAT | int4 | 81.20% | −8.95% | △ |
| 4 | STE (Conv QAT 全关 bug) | int4 | 74.35% | −15.80% | ✗ |
| 5 | Mixed | Conv int4 / Linear fp32 | 91.26% | +1.11% | ★ |
| 6 v2 | Phase4 修复 (QAT 全开) | int4 | 91.06% | +0.91% | ★ |
| 6 v3 | **Gazelle 匹配** (stem FP32 + Gazelle 噪声) | int8 | **92.06%** (干净 val) | +1.91% | ★★ |
| LSQ+ | LSQ+ 修复 (可学习 scale/zp) | int8 | 92.80% | +2.65% | ★ |
| — | **osimulator 真机 (全量 5400, 干净)** | int8 (stem 电计算) | **90.43%** | **+0.28%** | ★ |

**Model 2 轨迹要点**：int4 路线天花板 ~91%（受 stem 训练/推理不一致 + int4→int8 网格不对齐三重限制，详见 EXPERIMENTS §16）；切换到 **int8 + stem-FP32 + Gazelle 噪声**后 val 升至 92.06%；**真机全量 90.43%，反超原 FP32 基准 +0.28%**。

### 4.3 Model 3 (SpaceNet V2 + KD，FP32 基准 91.44%)

| Phase | 实现方式 | 参数精度 | 精度 | vs FP32 | 判定 |
|---|---|---|---|---|---|
| 0 | FP32 KD 基准 (ResNet-18 教师 97.83%) | fp32 | 91.44% | — | — |
| 1 | QAT 微调 | int4 | 73.22% | −18.22% | ✗ |
| 2 | 从零 KD+QAT | int4 | 83.26% | −8.18% | △ |
| 4 | KD+STE (Conv QAT 全关 bug) | int4 | 78.26% | −13.18% | ✗ |
| 5 | KD+Mixed | Conv int4 / Linear fp32 | 91.13% | −0.31% | ★ |
| 6 v2 | KD+Phase4 修复 | int4 | 91.50% | +0.06% | ★ |
| 6 v3 | **KD+Gazelle 匹配** | int8 | **91.83%** (干净 val) | +0.39% | ★★ |
| — | **osimulator 真机 (全量 5400, 干净)** | int8 (stem 电计算) | **90.28%** | −1.16% | ★ |

**Model 3 轨迹要点**：与 Model 2 同架构，全程叠加 KD（ResNet-18 教师）。v3 int8+KD val 91.83%；真机全量 90.28%，略低于其 KD 基准（因 KD 基准 91.44% 本就高于 M2 的 FP32 基准）。

### 4.4 三模型共同的方法论拐点

| 拐点 | 失败方案 | 成功方案 | 根因 |
|---|---|---|---|
| 微调 vs 从零 | FP32→int4 微调 (Phase 1, 全员 −11~−18%) | 从零 QAT (Phase 2) | FP32 权重依赖精细精度，突变量化破坏特征 |
| int4 vs int8 | int4 (16 级) 上限 ~91% | int8 (256 级, 匹配硬件原生) | int4→int8 网格不对齐 (max/7 vs max/127)，非无损 |
| stem 量化 | stem QAT (训练/推理分布不一致) | **stem 保留 FP32 电计算** | stem 对齐率仅 37.5%，电计算更划算且消除 BN 偏移 |
| 噪声建模 | STE 通用高斯噪声 | **Gazelle 噪声 (DAC ENOB 7.5 + TIA)** | 训练噪声匹配真实硬件 → 真机 gap 收敛到 ~1.6 点 |

---

## 5. 横向对比：五大维度同台对比

> 以下全部基于**干净 split** 的最终可部署配置（Model 2/3 = v3 int8；Model 1 = int8 v3 变体 A/B）。

### 5.1 参数量

| 模型 | 参数量 | 结构 | 相对规模 |
|---|---|---|---|
| Model 1 | **2.39M** | 6×Conv(3×3) + 2×Linear | 8.9× |
| Model 2 | **268K** | 4×Conv(1×1/2×2) + 2×Linear | 1× |
| Model 3 | **268K** | 同 Model 2 | 1× |

### 5.2 参数精度（部署配置）

| 模型 | 权重精度 | 激活精度 | 首层 | 备注 |
|---|---|---|---|---|
| Model 1 A | int8 | int8 | conv1_1 **FP32 电计算** | 光计算占比 97.74% |
| Model 1 B | int8 | int8 | conv1_1 + conv3_2 **FP32 电计算** | 光计算占比 73.64%，提速 ~24% |
| Model 2 | **int8** | int8 | stem **FP32 电计算** | 匹配硬件原生 8a8w |
| Model 3 | **int8** | int8 | stem **FP32 电计算** | 同 M2，外加 KD 训练 |

> 统一原则：**stem 首层 FP32 电计算（对齐率低）+ 其余层 int8 光计算**。所有 int8 权重均匹配 Gazelle 原生 8-bit，避免 int4→int8 重网格损失。

### 5.3 准确率

| 模型          | FP32 基准 | int8 val (干净) | int8 test (干净)    | **osim 真机 (全量/抽样)**  | 真机 vs FP32  |
| ----------- | ------- | ------------- | ----------------- | -------------------- | ----------- |
| Model 1 A   | 97.17%  | 97.87%        | 97.89%            | 98.00% (q50 抽样)      | +0.83%      |
| Model 1 B   | 97.17%  | 98.02%        | 97.96%            | 100.00% (q50 抽样)     | +2.83% (抽样) |
| **Model 2** | 90.15%  | 92.06%        | 92.20%            | **90.43% (全量 5400)** | **+0.28%**  |
| **Model 3** | 91.44%  | 91.83%        | fp32 test 92.13%¹ | **90.28% (全量 5400)** | −1.16%      |

¹ Model 3 的 `--qat` 推理路径走 int4 (84.59%)，给不了 int8 test 数；其 int8 test 数由 osimulator 真机取得 = 90.28%。

**真机 hardware gap**（osim 真机 − 干净 int8 QAT，同 test 集）：

| 模型 | int8 QAT test | osim 真机 | 纯硬件噪声 gap |
|---|---|---|---|
| Model 2 | 92.20% | 90.43% | **−1.77%** |
| Model 3 | (缺, 以 fp32 test 92.13% 推) | 90.28% | ~−1.6~1.8% |

→ 两模型真机 gap **基本相等 (~1.6 点)**，源自 osim 物理噪声 (DAC ENOB 7.5 + TIA σ=5.31)；Gazelle 噪声训练已吸收大部分，残留 ~1.6 点。

**精度目标达成情况**：

| 模型 | int8 目标 | int8 实际 (val) | 达成 |
|---|---|---|---|
| Model 1 | ≥ 96% | 97.87% / 98.02% | ✓ |
| Model 2 | ≥ 90% | 92.06% | ✓ |
| Model 3 | ≥ 91% | 91.83% | ✓ |

### 5.4 运行速率与计算开销

| 模型 | 总 MOPs/张 | 光计算占比 | 电计算层 | osim 单张耗时 | osim 全量 5400 | 训练耗时 |
|---|---|---|---|---|---|---|
| Model 1 A | **156.63M** | 97.74% | conv1_1 | ~150s 量级 (q50 抽样) | **~9 天 (不可行，仅抽样)** | 280 min |
| Model 1 B | 156.63M | 73.64% | conv1_1+conv3_2 | ~150s 量级 (提速 ~24%) | 同上 | 276 min |
| **Model 2** | **1.05M** | 90.65% | stem | **~2.5s** | **13357s (~3.7h)** | 72.8 min |
| **Model 3** | **1.05M** | 90.65% | stem | **~2.5s** | **13370s (~3.7h)** | 90.4 min |

**Model 2/3 真机全量统计**：引擎各调用 27000 次（5 光计算层 × 5400 张），总运算量 5.15e+09 MACs，硬件对齐率 99.6%。

**速率结论**：
- **Model 2/3 ≈ 0.4 张/秒（~24 张/分）**，全量 5400 张 ~3.7h 可一次跑完 → **可落地部署**。
- **Model 1 计算量是 Model 2/3 的 ~150×**（156.6M vs 1.05M），单张慢 ~60×（~150s vs ~2.5s），全量 ~9 天不可行 → 只能抽样验证（q50：A 98% / B 100%）。
- 光计算占比均 ≥ 90%（Model 1 A 97.7% / Model 2-3 90.65%），满足部署阈值。

> 注：osim 耗时为光计算硬件**物理仿真**时间（Python 模拟光学矩阵乘 + 补零对齐 + 物理噪声），反映模型→硬件映射的开销结构，并非最终芯片上墙钟时间。

### 5.5 五维横向对比总表

| 维度 | Model 1 | Model 2 | Model 3 |
|---|---|---|---|
| **实现方式** | 标准 VGG + Mixed/int8 QAT | 硬件对齐 SpaceNet + int8 QAT | 硬件对齐 SpaceNet + **KD** + int8 QAT |
| **参数量** | 2.39M | 268K | 268K |
| **参数精度** | int8 (首层 fp32) | int8 (stem fp32) | int8 (stem fp32) |
| **准确率 (真机)** | 98.0/100.0% (q50 抽样) | **90.43%** (全量) | **90.28%** (全量) |
| **运行速率** | ~150s/张, 全量不可行 | **~2.5s/张, 3.7h 全量** | **~2.5s/张, 3.7h 全量** |
| **光计算占比** | 97.7% (A) / 73.6% (B) | 90.65% | 90.65% |
| **可部署性** | △ (仅抽样) | ★★★ | ★★★ |

---

## 6. 部署推荐

| 场景 | 推荐 | 理由 |
|---|---|---|
| **轻量落地（首选）** | **Model 2 v3 int8** | 268K 参数、真机 90.43%、**反超 FP32 基准 +0.28%**、全量 3.7h 可验证 |
| **同等轻量 + KD** | Model 3 v3 int8+KD | 与 M2 真机打平 (90.28%)，若需 KD 故事线可选 |
| **最高精度（受算力）** | Model 1 int8 变体 B | val 98.02%、q50 真机 100%，但 2.39M 参数 + ~150s/张，仅适合算力充裕场景 |

**统一部署配置**：`stem FP32 电计算 + 其余 Conv/Linear int8 光计算 + Gazelle 噪声训练权重`，光计算占比 ≥ 90%，硬件对齐率 99.6%。

---

## 7. 关键技术要点（报告 Takeaways）

1. **硬件先于模型**：先逆向 Gazelle (8×2 / 8a8w / 线性度 99.4%)，再据此设计模型（2×2 conv 对齐 tile）与训练（int8 + Gazelle 噪声），而非训练后再迁就硬件。
2. **int8 > int4**：硬件原生 8-bit，int4 路线存在不可消除的网格不对齐损失（~6%）；int8 配方让真机 gap 收敛到 ~1.6 点。
3. **首层 FP32 是关键**：stem 对齐率仅 37.5%，保留电计算既高效又消除 BN 训练/推理分布偏移。
4. **从零 QAT > 微调**：低比特量化必须从零训练，让模型从未见过 fp32；微调路线全员失败。
5. **噪声匹配决定真机 gap**：训练时注入 Gazelle 物理噪声 (DAC ENOB 7.5 + TIA σ=5.3e-4)，真机精度基本保住。
6. **KD 未带来真机增益**：Model 2 (无 KD) 与 Model 3 (KD) 真机精度/gap 基本相等 → KD 提升噪声鲁棒性的假设**不被全量数据支持**。
7. **数据洁净性是底线**：Bug #11 (test⊂train 泄漏) 曾让 osim 虚高 ~3 点（93.28% / 93.26% 作废）；干净 split 后真值 90.43% / 90.28%。

---

## 附：数据来源与可信度

| 数据类型 | 来源 | 可信度 |
|---|---|---|
| 训练 val / test (QAT) | PyTorch 伪量化，干净 split | ✓ 干净泛化数 |
| osim 真机 (Model 2/3) | **全量 5400**，COMPASS 8a8w12o 仿真 | ✓ 真值（`log_optic_int8_new.md` / `log_optic_kd_new.md`） |
| osim 真机 (Model 1) | q50 抽样（全量 ~9 天不可行） | △ 小样本，仅作机制验证 |
| 历史 leaky osim (93.28/93.26%) | Bug #11 前 | ✗ 作废 |

> 完整实验轨迹、Bug 记录、int4 困境分析见 `EXPERIMENTS.md`（§11.13 为 Model 2/3 全量真机定稿）。
