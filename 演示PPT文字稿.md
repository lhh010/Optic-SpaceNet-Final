# 演示 PPT 文字稿 · Optic-SpaceNet 光学 CNN 加速系统

> 面向航空航天"在轨计算"的光学 CNN 加速系统
> 总时长 **≈ 10 分钟** · 共 **12 页** · 每页标注时间、版面内容、图表、口述讲稿
> 评分重心映射：**光计算占比 (20分)** → 第 9 页；**进阶 (20分)** → 第 11–12 页；**AI 工具链 (10分)** → 第 7 页；**创新性 (5分)** → 第 6 页。

---

## 第 1 页 · 封面 （0:00–0:25，约 25 秒）

**版面内容**
- 主标题：**面向航空航天在轨计算的光学 CNN 加速系统 —— Optic-SpaceNet**
- 副标题：三模型量化迁移 × Gazelle 光计算平台 × 知识蒸馏
- 团队成员 / 指导老师 / 单位 / 日期

**图表/视觉**
- 背景：卫星 + 光子芯片 + 遥感地物缩略图（AnnualCrop/Forest/River 拼贴）
- 角标：EuroSAT 10 分类 icon 一行

**口述讲稿**
> "各位评委好，我们带来的作品是 Optic-SpaceNet——一套面向卫星在轨遥感计算的光学 CNN 加速系统。我们把三种 CNN 迁移到曦智 Gazelle 光计算硬件上，在保持 90% 以上准确率的同时，把超过 90% 的乘加运算交给了光子完成。下面用 10 分钟汇报。"

---

## 第 2 页 · 问题与动机 （0:25–1:15，约 50 秒）

**版面内容**
- **在轨计算痛点**：卫星遥感数据海量、星地下行带宽受限 → 必须在星上完成实时分类
- **电子计算瓶颈**：GPU/ASIC 功耗高、重量大、散热难，不适合卫星平台
- **光子计算优势**：光速并行矩阵乘、低功耗、抗辐射
- **题目目标**：把 CNN 迁移到 Gazelle 8×2 光学矩阵乘法器，最大化光计算占比，量化精度可控

**图表/视觉**
- 左：星上推理流程图（光学相机 → 光子芯片 → 地物分类结果）
- 右：电子 vs 光子计算对比小表（功耗 / 并行度 / 抗辐射）

**口述讲稿**
> "卫星每圈过境产生海量遥感图像，全下传不现实，必须在星上就地分类。传统电子计算功耗重量都太大；光子计算用光做矩阵乘，低功耗、高并行、还抗辐射，天然适合在轨场景。题目要求我们把 CNN 跑在曦智 Gazelle 光计算芯片上，光计算占比越高越好——这正是我们工作的核心。"

---

## 第 3 页 · 硬件平台 Gazelle （1:15–2:00，约 45 秒）【平台使用 5分】

**版面内容**
- 物理 tile **8×2**（k=8, n=2）→ im2col 展平长度须被 **8 整除**
- 原生精度 **8a8w12o**：8-bit 激活 / 8-bit 权重 / 12-bit 输出
- 硬件线性度 **99.4%**（相对误差仅 0.6%）、DAC ENOB 7.5、TIA 噪声 σ≈5.3e-4
- **关键结论**：硬件近乎理想 → 量化与噪声建模是唯一精度瓶颈

**图表/视觉**
- 8×2 光学 tile 结构示意图（输入光 → 调制 → 光学 MAC → 探测）
- 硬件参数表（逆向分析自 `GAZELLE_ARCHITECTURE.md` + calibration_params）

**口述讲稿**
> "我们先逆向分析了 Gazelle 硬件：它的物理计算单元是 8×2 tile，要求卷积展平后被 8 整除；原生支持 int8 权重。最关键的发现是——硬件线性度高达 99.4%，几乎理想。这意味着所有精度损失都来自量化，而不是硬件缺陷。这个判断决定了我们后续所有训练策略。"

---

## 第 4 页 · 三模型设计 （2:00–3:00，约 60 秒）【方案设计 / 创新】

**版面内容**
- **Model 1 Baseline VGG**：标准 3×3 conv，2.39M 参数，展平长度=9 无法被 8 整除 → 计算量巨大（156M MOPs）
- **Model 2 Optic-SpaceNet V1**：硬件对齐设计（2×2 conv，展平=8 的倍数），268K 参数
- **Model 3 Optic-SpaceNet V2**：同架构 + ResNet-18 知识蒸馏（教师 97.83%）
- 设计哲学：**用 1/9 的参数换 1/150 的计算量**；全层 bias=False 匹配光硬件

**图表/视觉**
- 三模型架构对比表（结构 / 参数量 / 硬件对齐率 / 总 MOPs）

| | Model 1 | Model 2 | Model 3 |
|---|---|---|---|
| 结构 | 6×Conv(3×3)+2×FC | 4×Conv(1×1/2×2)+2×FC | 同 M2 |
| 参数量 | 2.39M | 268K | 268K |
| 对齐率 | 99.8% | 99.6% | 99.6% |
| MOPs/张 | 156.6M | **1.05M** | **1.05M** |

**口述讲稿**
> "我们设计了三个模型做对比消融。Model 1 是标准 VGG，精度高但 3×3 卷积展平长度 9，不对齐 8×2 tile，计算量是另外两个的 150 倍——在卫星上根本跑不动。Model 2、3 专门为硬件对齐设计，用 2×2 卷积让展平长度完美被 8 整除，只用 268K 参数、1M 运算量。Model 3 在此基础上加了知识蒸馏。核心思路是用九分之一的参数换一百五十分之一的计算量。"

---

## 第 5 页 · 方法演进路线 （3:00–4:00，约 60 秒）【AI工具链 / 技术复杂度】

**版面内容**
- Phase 1 QAT 微调（FP32→低比特）→ **全员失败**（−11~−18%）
- Phase 2 从零 QAT → 有效，但仍差 6–9%
- Phase 4 STE + 噪声注入 → int4 达 96.46%（Model 1）
- Phase 5 Mixed 精度 → 反超 FP32 基准
- **Phase 6 定型配方**：int8 权重 + **Gazelle 物理噪声训练** + stem 首层 FP32
- 方法论：**硬件先于模型**——训练时就匹配硬件，而非训练后再迁就

**图表/视觉**
- 📈 **精度演进折线图（建议新建）**：三模型准确率随 Phase 1→6 单调爬升曲线，标注失败/成功拐点

**口述讲稿**
> "量化这条路我们迭代了六个阶段。最早想用 FP32 模型直接微调到 int4，三个模型全部崩盘，掉 11 到 18 个点。后来改成从零训练量化、引入 STE 和噪声注入，逐步把 int4 损失压到 1 个点以内。最终在第六阶段定型：用 int8 权重匹配硬件原生精度，训练时注入 Gazelle 真实物理噪声，首层保留 FP32。这条演进线背后是一个核心方法论——硬件先于模型，训练阶段就让模型适应硬件。"

---

## 第 6 页 · 核心创新点 （4:00–4:45，约 45 秒）【创新性 5分】

**版面内容**（四大创新）
1. **硬件匹配训练**：训练期注入 Gazelle 真实噪声（DAC ENOB 7.5 + TIA），真机精度 gap 仅 ~1.6 点
2. **stem FP32 电计算策略**：首层对齐率仅 37.5%，保留电计算，既高效又消除 BN 训练/推理偏移
3. **int8 全流程对齐**：训练权重 = 硬件原生 8a8w，避免 int4→int8 网格不对齐损失
4. **硬件感知架构**：2×2 卷积完美对齐 8×2 tile，综合对齐率 99.6%

**图表/视觉**
- 四宫格 icon 图（每个创新点一个图标 + 一句话）
- 或：训练↔推理一致性 checklist（stem / int8 / 噪声 三项对勾）

**口述讲稿**
> "我们的创新可以总结为四点。最核心的是硬件匹配训练——一般做法是训练完再量化部署，精度必然掉；我们在训练时就注入芯片的真实物理噪声，让模型提前适应，最终真机精度只比训练低 1.6 个点。其次是首层 FP32 电计算策略、int8 全流程对齐、以及硬件感知的 2×2 卷积架构。这四点共同保证了高光计算占比下的高精度。"

---

## 第 7 页 · 训练与量化工具链 （4:45–5:30，约 45 秒）【AI工具链 10分】

**版面内容**（完整闭环）
- **数据**：EuroSAT 27000 张 → 干净三分 split（train 16200 / val 5400 / test 5400），修复 test⊂train 泄漏
- **训练**：QAT（STE / LSQ+），100 epoch，Warmup+Cosine 调度，Gazelle 噪声注入
- **量化**：int8 权重 + int8 激活，per-channel 量化
- **部署**：QAT 权重 → osimulator 8a8w 编译 → 光计算推理（OpticConv2d/OpticLinear）
- 工具链：**数据 → 训练 → 量化 → 编译 → 光计算部署** 全自动闭环

**图表/视觉**
- 🔄 工具链横向流程图（5 个阶段方框 + 箭头，标注每阶段产出：.pth → int8 → osim）

**口述讲稿**
> "工具链是完整闭环。数据处理上我们用 EuroSAT 两万七千张图，做了严格的三分划分，并修复了一个测试集泄漏进训练集的严重 bug。训练采用量化感知训练 QAT，一百个 epoch，配合余弦退火和 Gazelle 噪声。量化成 int8 权重和激活后，经过 osimulator 的 8a8w 编译，直接部署到光计算路径推理。从数据到光计算部署，全自动打通。"

---

## 第 8 页 · 精度结果 （5:30–6:30，约 60 秒）【功能及性能 10分】

**版面内容**
- 三模型 int8 精度（干净 val）：**M1 97.87/98.02% · M2 92.06% · M3 91.83%**，全部达成 int8 目标
- **量化无损甚至反超**：M2 vs FP32 基准 +1.91%，M3 +0.39%
- **osimulator 真机全量 5400 张**：M2 **90.43%** · M3 **90.28%**
- Model 2 真机**反超原 FP32 基准 +0.28%**

**图表/视觉**
- 📊 **精度对比柱状图（建议新建）**：每模型三根柱 = FP32 基准 / int8 val / osim 真机
- 或直接用 `docs/figures/compute_vs_accuracy_final.png`（计算量-精度散点，已有）

**口述讲稿**
> "这是精度结果。三个模型在干净验证集上 int8 精度分别达到 98%、92%、92%，全部超过我们设定的 int8 目标，而且量化几乎无损——Model 2 甚至比 FP32 基准还高近两个点。更重要的是真机验证：在 osimulator 真实光计算硬件上跑完全部 5400 张测试图，Model 2 达到 90.43%，反超了原始 FP32 模型。这说明我们的量化模型在真实光子硬件上是真正可用的。"

---

## 第 9 页 · 光计算占比（重点） （6:30–7:20，约 50 秒）【⭐ 基础要求 20 分 —— 最大单项】

**版面内容**
- **光计算占比**：Model 1 (A) **97.74%** · Model 2 **90.65%** · Model 3 **90.65%**（均 ≥ 90% 部署阈值）
- **硬件对齐率 99.6%**：除 stem 外所有光计算层完美对齐 8×2 tile，**零补零浪费**
- 6 层中 **5 层走光计算**，仅 stem 首层电计算
- 光计算 MOPs 分布：stage1 49.9% / fc1 24.9% / stage2 12.5% / stage3 3.1% / fc2 0.2%

**图表/视觉**
- 🥧 **光计算占比饼图 + 电计算小扇区**（建议新建）
- 📊 MOPs 层级分布堆叠柱状图（每层 光计算/电计算 拆分）

**口述讲稿**
> "这一页是评分最看重的光计算占比。我们三个模型的光计算占比分别是 97.7%、90.65%、90.65%，全部超过 90% 的部署阈值。能做到这么高，是因为我们用 2×2 卷积让每一层的展平长度都完美被 8 整除，硬件对齐率达到 99.6%，几乎没有补零浪费。六层网络里五层完全在光计算上跑，只有对齐率太低的首层保留在电计算。也就是说，超过九成的乘加运算真正交给了光子完成。"

---

## 第 10 页 · 计算效率与在轨部署 （7:20–8:20，约 60 秒）【实用性 5分 + 功能性能】

**版面内容**
- **Model 2/3**：1.05M MOPs/张、~2.5s/张、268K 参数 → **在轨轻量部署首选**
- **Model 1**：156M MOPs（**150×**）、~150s/张 → 算力/功耗过高，不适合卫星
- 全量 5400 张 ~3.7h 跑完，引擎调用 27000 次
- **部署推荐**：Model 2 v3 int8（真机 90.43%、反超 FP32、268K 轻量）

**图表/视觉**
- 📉 **`docs/figures/compute_vs_accuracy_final.png`（已有，核心图）**：横轴计算量、纵轴精度，三模型散点，Model 2/3 在左上角（低算力高精度）

**口述讲稿**
> "回到在轨场景的核心诉求——低功耗、低延迟。这张图横轴是计算量、纵轴是精度。Model 1 虽然精度最高，但计算量是 Model 2、3 的 150 倍，单张图要跑两分多钟，卫星上根本用不了。Model 2、3 只用 1M 运算量、单张两秒半，却能达到 90% 以上精度，落点在图的左上角——低算力高精度，正是卫星在轨部署的最优解。"

---

## 第 11 页 · 鲁棒性与真机验证 （8:20–9:10，约 50 秒）【功能性能 / 进阶】

**版面内容**
- **噪声鲁棒性**：训练时注入 Gazelle 噪声 → 真机 gap 仅 ~1.6 点（M2/M3 基本相等）
- **两级验证体系**：QAT 秒级交叉验证 + osimulator 真机全量 5400（小时级）
- **科学诚信**：主动发现并修复 Bug #11（test⊂train 泄漏），重训 + 真机复测，所有数字干净可信
- **发现**：KD（Model 3）真机未显著优于 Model 2 → 真机鲁棒性主要来自 Gazelle 噪声训练

**图表/视觉**
- 📈 `docs/figures/noise_robustness_v2.png`（已有，噪声扫描曲线）
- 小表：真机 vs val gap（M2 −1.63% / M3 −1.55%，基本相等）

**口述讲稿**
> "为验证可靠性，我们做了两级验证：秒级的 QAT 交叉验证加上小时级的 osimulator 真机全量测试。真机精度只比训练低 1.6 个点，说明我们的噪声匹配训练有效。这里特别提一点科学诚信：我们主动发现并修复了测试集泄漏进训练集的 bug，重训并复测，现在所有数字都是干净的。还有一个有意思的发现——加了知识蒸馏的 Model 3 真机并不比 Model 2 强，说明真机鲁棒性主要来自噪声训练，而非蒸馏。"

---

## 第 12 页 · 总结与展望 （9:10–10:00，约 50 秒）【总结 / 进阶 20分】

**版面内容**
- **成果总览**：三模型全部完成光计算迁移，光计算占比 ≥90%，真机 90%+，量化无损
- **进阶贡献**：① int8 全流程真机部署（osim 全量验证）② Gazelle 噪声匹配训练（近无损真机）③ 三模型 × 多量化方案完整消融 ④ 在轨遥感应用闭环
- **方法论**：硬件先于模型 → 硬件匹配训练 → 真机验证闭环
- **展望**：光电混合提速（H1/H2/H3）、更大模型补偿、端到端硬件在环训练、真实卫星在轨验证

**图表/视觉**
- 成果总表（一页纸 KPI：光计算占比 / 真机精度 / 参数量 / MOPs）
- 路线图：当前 → 光电混合 → 在轨验证

**口述讲稿**
> "总结一下：我们把三种 CNN 全部迁移到光计算，光计算占比超过 90%，真机精度 90% 以上，量化几乎无损。进阶方面，我们实现了 int8 全流程真机部署、用噪声匹配训练做到近无损真机推理、并完成了三模型多量化的完整消融。方法论上始终坚持'硬件先于模型'。下一步我们计划做光电混合加速、并推动真实卫星在轨验证。以上是我们的汇报，感谢各位评委，请提问。"

---

## 附：建议制作的图表清单（配图用）

| 图表                 | 用途        | 状态                                                |
| ------------------ | --------- | ------------------------------------------------- |
| 计算量-精度散点图          | 第 10 页核心图 | ✅ 已有 `docs/figures/compute_vs_accuracy_final.png` |
| int4 噪声鲁棒性曲线       | 第 11 页    | ✅ 已有 `docs/figures/noise_robustness_v2.png`       |
| 精度演进折线图（Phase 1→6） | 第 5 页     | ⏳ 建议新建                                            |
| 光计算占比饼图 + MOPs 分布  | 第 9 页     | ⏳ 建议新建                                            |
| 三模型精度对比柱状图         | 第 8 页     | ⏳ 建议新建（或复用散点图）                                    |
| 工具链流程图             | 第 7 页     | ⏳ 建议新建                                            |
| 8×2 tile / 卫星在轨示意  | 第 1/3 页   | ⏳ 建议新建（可用 icon 拼图）                                |

> 讲解节奏提示：第 3、9 页是"平台使用 / 光计算占比"两个高分项，务必讲透；第 5、6 页体现技术复杂度与创新；第 8、10 页用数据说话。控制在 10 分钟内，留 5 分钟答辩。
